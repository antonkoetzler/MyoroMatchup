name: myoro_matchup
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  myoro_flutter_library:
    path: ../../myoro_flutter_library
  myoro_flutter_annotations: ^1.2.2
  provider: ^6.1.5
  get_it: ^8.0.3
  injectable: ^2.5.0
  faker: 2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.5.4
  injectable_generator: ^2.7.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
