import 'package:flutter/material.dart';
import 'package:myoro_flutter_library/myoro_flutter_library.dart';

/// [EdgeInsets] length.
const kEdgeInsetsLength = kMyoroMultiplier * 3;

/// [MyoroAppBar.showBottomDivider] value.
const kAppBarBordered = true;

/// Transparency opacity.
const kTransparencyOpacity1 = kMyoroMultiplier * 4 / 100;
const kTransparencyOpacity2 = kMyoroMultiplier * 5 / 100;
const kTransparencyOpacity3 = kMyoroMultiplier * 6 / 100;

/// [Duration] in success scenarios where we need to
/// add a delay before navigation out of said screen
const kSuccessNavigationDelayDuration = Duration(milliseconds: 500);
