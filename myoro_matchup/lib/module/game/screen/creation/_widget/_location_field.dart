part of '../game_creation_screen.dart';

/// Location field of [GameCreationScreen].
final class _LocationField extends StatelessWidget {
  const _LocationField();

  @override
  Widget build(_) {
    return const Text('LocationField');
  }
}
