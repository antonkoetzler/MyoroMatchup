part of '../game_creation_screen.dart';

/// [MyoroScreenConfiguration.body] of [GameCreationScreen].
final class _Body extends StatelessWidget {
  const _Body();

  @override
  Widget build(context) {
    return const Center(child: Text('Bruh'));
  }
}
