# OAuth Provider Configuration for Supabase Local Development
# These environment variables are read by Supabase when running 'supabase start'
# They are referenced in config.toml using env() syntax
# 
# To use these:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your OAuth provider credentials
# 3. Export variables before starting Supabase, or use dotenvx
#
# For local development, add these redirect URLs to your OAuth provider dashboards:
# - Google: http://127.0.0.1:54321/auth/v1/callback
# - GitHub: http://127.0.0.1:54321/auth/v1/callback  
# 

SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
SUPABASE_AUTH_EXTERNAL_GITHUB_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GITHUB_SECRET=
