# make_localization

Accepts various referenced files (i.e. `/make_localization file1.dart file2.dart fileN.dart`)

Generate localization for all langauges for the files specified. Usually, the files specified will have an error of the missing localization that hasen't been implemented yet. For Flutter, run pub get after you do this.
